<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.cas.MainActivity" >
    
 
   <!--ListView
       android:id="@+id/announcements"
       android:layout_width="match_parent"
       android:layout_height="wrap_content"   
        
        android:textColor="#FFFFFF"
       android:layout_alignLeft="@+id/etResponse"
       android:layout_alignParentBottom="true"
       android:layout_below="@+id/etResponse"
       android:layout_marginTop="25dp">
       

   </ListView-->
<ListView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@android:id/list" />

   <ProgressBar
       android:id="@+id/progressBar"
       style="?android:attr/progressBarStyleLarge"
       android:layout_width="wrap_content"
       android:layout_height="wrap_content"
       android:layout_alignLeft="@+id/announcements"
       android:layout_alignParentBottom="true"
       android:layout_alignRight="@+id/announcements"
       android:layout_alignTop="@+id/announcements"
       android:layout_marginBottom="136dp"
       android:layout_marginLeft="116dp"
       android:layout_marginTop="116dp"
       android:layout_marginRight="116dp"
       android:visibility="invisible" />
    
</RelativeLayout>
